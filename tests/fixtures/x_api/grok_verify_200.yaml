# Phase 9 fixture cassette — successful GET /2/tweets/{id} for §29.2
# Grok-candidate verification. Loaded by tests/_xurl_fixture.py via
# the subprocess.run patcher. The response data shape matches what
# X API actually returns: a single tweet object with public_metrics,
# created_at, and text fields.
interactions:
  - request:
      method: GET
      uri: /2/tweets/
    response:
      status_code: 200
      body:
        data:
          id: "1234567890"
          text: "Home cooks are starting to push back on meal-plan apps that just spam recipes."
          author_id: "777000111"
          created_at: "2026-05-22T22:00:00Z"
          public_metrics:
            like_count: 42
            reply_count: 3
            retweet_count: 1
            quote_count: 0
            bookmark_count: 7
            impression_count: 1200
      stderr: ""
      exit_code: 0
